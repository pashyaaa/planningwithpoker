import { Container } from '@mui/material';
import Header from './components/Header';

const App = () => {
    return (
        <Container>
            <Header></Header>
        </Container>
    );
};

export default App;
